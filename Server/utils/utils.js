export function normalizeString(inputString) {
    if (inputString) {
        return inputString.toLowerCase().trim();

    }
    // Chuyển chuỗi về chữ thường và loại bỏ các khoảng trắng ở đầu và cuối chuỗi
}
